# Secrets / credentials — never commit
Neo4j-*.txt
*.credentials
*.secrets
.env
.env.*
!.env.example

# Python virtual environment
.venv/
venv/
ENV/
env/

# JetBrains IDEs
.idea/

# Python bytecode and cache
__pycache__/
*.py[cod]
*$py.class
*.so

# Local downloaded CLI tools
.tools/

# Generated research datasets — track only the layout README; ignore everything else
data/*
!data/README.md

# Distribution / packaging
build/
dist/
*.egg-info/

temp-dir/

datav4.tar.gz
.claude/*

logs/
pre_data_logs.tar.gz
post_data_logs.tar.gz
data_logs.tar.gz
DOCS/docs2/presentation.md

# Experiment outputs (raw artifacts of pipeline runs).
# All headline numbers are persisted in the paper drafts (RESEARH-PAPER/)
# and the docs (docs4/, docs6/, docs7/); raw outputs are reproducible
# from scripts/ and are not tracked.
results/

microservices-demo-google/
# OTel Demo fork — vendored as reference only, never edited (per docs5/01 Rule R2).
# We deploy via published images + the official helm chart; this directory is read-only ref.
opentelemetry-demo/

temp
.claude

DOCS/icse-planned.md
data.tar.gz
# paper-results: commit metrics/summaries; large per-window predictions ship as a release archive
paper-results/**/*-predictions.jsonl
paper-results/**/traces/
paper-results/**/*-cells/
paper-results/**/*.jsonl
