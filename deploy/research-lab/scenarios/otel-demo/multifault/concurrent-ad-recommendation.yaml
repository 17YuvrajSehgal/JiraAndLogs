schema_version: 1
scenario_id: otel-demo-concurrent-ad-recommendation
title: L2 concurrent — ad outage + recommendation outage (personalization-layer)
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components: [ad, recommendation, frontend]
  labels: [synthetic-incident, multi-fault, concurrent, L2, personalization, telemetry-linked]
  summary_template: "Personalization layer broken — ads + recs both failing"
  description_template: |
    Synthetic L2 scenario. Both ad and recommendation fail simultaneously
    via different mechanisms (flagd adFailure + ScaleDeployment rec=0).
    Tests semantic clustering: both faults are in the "personalization"
    cluster but distinct services.
fault:
  fault_id: multifault-concurrent-003
  fault_type: multi_fault_concurrent
  affected_service: ad+recommendation
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: true, expected_error_rate: medium, expected_latency_impact: low}
labels:
  severity: major
  incident_type: degradation
  root_cause_category: multi_fault_concurrent
  should_alert: true
  should_create_jira_shadow_issue: true
  difficulty_level: L2
execution:
  action: MultiFault
  namespace: otel-demo-research
  composition_type: concurrent
  components_file: deploy/research-lab/scenarios/otel-demo/multifault/components/concurrent-ad-recommendation.json
triage:
  schema_version: 1
  scenario_family: multifault-concurrent
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Both healthy."}
    active_fault: {triage_label: ticket_worthy, triage_severity: major, triage_components: [ad, recommendation], triage_reason_class: multi_fault_concurrent, is_hard_case: true, rationale: "Personalization layer broken via two mechanisms."}
    recovery_window: {triage_label: borderline, triage_severity: minor, triage_components: [ad, recommendation], triage_reason_class: multi_fault_concurrent, is_hard_case: true, rationale: "Recovery."}
