schema_version: 1
scenario_id: otel-demo-concurrent-currency-shipping
title: L2 concurrent — currency outage + shipping outage simultaneously
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Critical
  components: [currency, shipping, checkout]
  labels: [synthetic-incident, multi-fault, concurrent, L2, telemetry-linked]
  summary_template: "Concurrent currency + shipping outage — checkout double-broken"
  description_template: |
    Synthetic L2 (concurrent dual-fault) scenario.
    Both currency and shipping scaled to 0 simultaneously. Checkout fails in
    multiple ways. Two non-overlapping gold tickets in memory.
fault:
  fault_id: multifault-concurrent-002
  fault_type: multi_fault_concurrent
  affected_service: currency+shipping
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: true, expected_error_rate: high, expected_latency_impact: medium}
labels:
  severity: critical
  incident_type: outage
  root_cause_category: multi_fault_concurrent
  should_alert: true
  should_create_jira_shadow_issue: true
  difficulty_level: L2
execution:
  action: MultiFault
  namespace: otel-demo-research
  composition_type: concurrent
  components_file: deploy/research-lab/scenarios/otel-demo/multifault/components/concurrent-currency-shipping.json
triage:
  schema_version: 1
  scenario_family: multifault-concurrent
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Both healthy."}
    active_fault: {triage_label: ticket_worthy, triage_severity: critical, triage_components: [currency, shipping], triage_reason_class: multi_fault_concurrent, is_hard_case: true, rationale: "Two simultaneous outages."}
    recovery_window: {triage_label: borderline, triage_severity: minor, triage_components: [currency, shipping], triage_reason_class: multi_fault_concurrent, is_hard_case: true, rationale: "Recovery."}
