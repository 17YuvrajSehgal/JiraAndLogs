schema_version: 1
scenario_id: otel-demo-concurrent-productcatalog-latency-flapping-pod
title: L2 concurrent — productcatalog latency + flapping cart pod
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components: [product-catalog, cart]
  labels: [synthetic-incident, multi-fault, concurrent, L2, mixed-severity, telemetry-linked]
  summary_template: "Catalog slow + cart flapping at the same time"
  description_template: |
    Synthetic L2 scenario. One severe (productcatalog latency 750ms) + one
    noisy (cart pod flapping). Mixed-severity composition; tests whether
    retrieval pulls back both golds even though they have different
    triage severity.
fault:
  fault_id: multifault-concurrent-004
  fault_type: multi_fault_concurrent
  affected_service: product-catalog+cart
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: true, expected_error_rate: medium, expected_latency_impact: high}
labels:
  severity: major
  incident_type: degradation
  root_cause_category: multi_fault_concurrent
  should_alert: true
  should_create_jira_shadow_issue: true
  difficulty_level: L2
execution:
  action: MultiFault
  namespace: otel-demo-research
  composition_type: concurrent
  components_file: deploy/research-lab/scenarios/otel-demo/multifault/components/concurrent-productcatalog-latency-flapping-pod.json
triage:
  schema_version: 1
  scenario_family: multifault-concurrent
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Both healthy."}
    active_fault: {triage_label: ticket_worthy, triage_severity: major, triage_components: [product-catalog, cart], triage_reason_class: multi_fault_concurrent, is_hard_case: true, rationale: "Mixed-severity composition."}
    recovery_window: {triage_label: borderline, triage_severity: minor, triage_components: [product-catalog, cart], triage_reason_class: multi_fault_concurrent, is_hard_case: true, rationale: "Recovery."}
