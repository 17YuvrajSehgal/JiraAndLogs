{
  "_comment": "L3 cascade: currency outage PRIMARY -> frontend display errors SECONDARY (emergent). Depth 2 (shallow).",
  "cascade_emergence_window_seconds": 30,
  "fault_components": [
    {"action": "ScaleDeployment", "target_name": "currency", "replicas": 0, "restore_replicas": 1, "is_primary_cause": true, "cascade_depth": 0, "service_role": "fx-conversion"},
    {"action": "observation_only", "expected_target": "frontend", "expected_signal": "frontend's price-format spans throw errors when currency.Convert calls fail; frontend renders fallback UI or 500s depending on degradation path", "is_primary_cause": false, "cascade_depth": 1, "service_role": "web-ui"}
  ]
}
