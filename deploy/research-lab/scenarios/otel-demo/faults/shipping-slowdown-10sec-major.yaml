schema_version: 1
scenario_id: otel-demo-shipping-slowdown-10sec-major
title: International shipping slowdown — 10sec delay (flagd intensity)
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components: [shipping, checkout]
  labels: [synthetic-incident, latency, shipping, telemetry-linked, intensity-high]
  summary_template: "International shipping showing 10sec delay"
  description_template: |
    Synthetic lab issue from scenario otel-demo-shipping-slowdown-10sec-major.
    flagd intlShippingSlowdown=10sec; checkout shipping step blocked 10s+
    on international orders. Likely to time out upstream.
fault:
  fault_id: shipping-slow-10s-001
  fault_type: latency_injection
  affected_service: shipping
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: true, expected_error_rate: medium, expected_latency_impact: high}
labels:
  severity: major
  incident_type: degradation
  root_cause_category: latency_injection
  should_alert: true
  should_create_jira_shadow_issue: true
execution:
  action: Flagd
  namespace: otel-demo-research
  flagd_flag: intlShippingSlowdown
  flagd_variant: "10sec"
triage:
  schema_version: 1
  scenario_family: productcatalog-latency
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Pre-fault; shipping normal."}
    active_fault: {triage_label: ticket_worthy, triage_severity: major, triage_components: [shipping, checkout], triage_reason_class: latency_injection, is_hard_case: false, rationale: "10sec sustained delay; user impact."}
    recovery_window: {triage_label: borderline, triage_severity: minor, triage_components: [shipping], triage_reason_class: latency_injection, is_hard_case: true, rationale: "Recovery; latency tail."}
