schema_version: 1
scenario_id: otel-demo-recommendation-outage-major
title: Recommendation service outage — empty rec carousel
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components: [recommendation, frontend]
  labels: [synthetic-incident, service-outage, recommendation, telemetry-linked]
  summary_template: "Recommendation service outage — empty rec carousel"
  description_template: |
    Synthetic lab issue generated from scenario otel-demo-recommendation-outage-major.

    The recommendation service was scaled to 0. Frontend's "you may also like"
    section is empty. Degraded UX but not order-blocking.
fault:
  fault_id: recommendation-outage-001
  fault_type: service_outage
  affected_service: recommendation
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: true, expected_error_rate: medium, expected_latency_impact: low}
labels:
  severity: major
  incident_type: outage
  root_cause_category: service_unavailable
  should_alert: true
  should_create_jira_shadow_issue: true
execution:
  action: ScaleDeployment
  namespace: otel-demo-research
  target_kind: deployment
  target_name: recommendation
  replicas: 0
  restore_replicas: 1
triage:
  schema_version: 1
  scenario_family: recommendation-outage
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Pre-fault; recommendation healthy."}
    active_fault: {triage_label: ticket_worthy, triage_severity: major, triage_components: [recommendation, frontend], triage_reason_class: service_unavailable, is_hard_case: false, rationale: "Rec service down; carousel empty."}
    recovery_window: {triage_label: borderline, triage_severity: minor, triage_components: [recommendation], triage_reason_class: service_unavailable, is_hard_case: true, rationale: "Recovery; cache rebuild."}
