schema_version: 1
scenario_id: otel-demo-email-memory-leak-1000x-critical
title: Email service memory leak (1000x intensity) — OOMKill imminent
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Critical
  components: [email]
  labels: [synthetic-incident, resource-leak, email, telemetry-linked, intensity-critical, oom]
  summary_template: "Email service rapid memory leak — repeated OOMKills"
  description_template: |
    Synthetic lab issue from scenario otel-demo-email-memory-leak-1000x-critical.
    flagd emailMemoryLeak=1000x; pod RSS climbs fast; OOMKilled within minutes;
    pod restarts and leaks again. Crash loop visible.
fault:
  fault_id: email-memleak-1000x-001
  fault_type: resource_leak
  affected_service: email
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: false, expected_error_rate: medium, expected_latency_impact: low}
labels:
  severity: critical
  incident_type: outage
  root_cause_category: resource_leak
  should_alert: true
  should_create_jira_shadow_issue: true
execution:
  action: Flagd
  namespace: otel-demo-research
  flagd_flag: emailMemoryLeak
  flagd_variant: "1000x"
triage:
  schema_version: 1
  scenario_family: slow-leak-saturation
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Pre-fault."}
    active_fault: {triage_label: ticket_worthy, triage_severity: critical, triage_components: [email], triage_reason_class: resource_leak, is_hard_case: false, rationale: "Rapid OOM crash loop; immediate action required."}
    recovery_window: {triage_label: borderline, triage_severity: minor, triage_components: [email], triage_reason_class: resource_leak, is_hard_case: true, rationale: "Recovery; pod stable after flag off."}
