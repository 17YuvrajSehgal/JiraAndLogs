schema_version: 1
scenario_id: otel-demo-shipping-slowdown-5sec-minor
title: International shipping slowdown — 5sec delay (chaos-mesh network latency)
dataset_run_id: otel-demo-placeholder-run
traffic_profile_id: otel-demo-baseline-checkout-mix
environment: research-otel-demo
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Minor
  components: [shipping]
  labels: [synthetic-incident, latency, shipping, telemetry-linked, intensity-low]
  summary_template: "International shipping showing 5sec delay"
  description_template: |
    Synthetic lab issue from scenario otel-demo-shipping-slowdown-5sec-minor.
    chaos-mesh NetworkChaos delay (~5s) on shipping; checkout's shipping step
    takes ~5s extra. Borderline ticket-worthy.
fault:
  fault_id: shipping-slow-5s-001
  fault_type: latency_injection
  affected_service: shipping
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius: {user_visible: true, expected_error_rate: low, expected_latency_impact: medium}
labels:
  severity: minor
  incident_type: degradation
  root_cause_category: latency_injection
  should_alert: false
  should_create_jira_shadow_issue: true
execution:
  action: ChaosMeshChaos
  namespace: otel-demo-research
  chaos_manifest: deploy/research-lab/scenarios/otel-demo/chaos/manifests/network-latency-shipping-5sec.yaml
triage:
  schema_version: 1
  scenario_family: network-latency
  per_window:
    pre_fault_baseline: {triage_label: noise, is_hard_case: false, rationale: "Pre-fault; shipping normal."}
    active_fault: {triage_label: borderline, triage_severity: minor, triage_components: [shipping], triage_reason_class: latency_injection, is_hard_case: true, rationale: "5sec extra latency; possibly tolerable."}
    recovery_window: {triage_label: noise, is_hard_case: false, rationale: "Chaos removed; normal latency."}
