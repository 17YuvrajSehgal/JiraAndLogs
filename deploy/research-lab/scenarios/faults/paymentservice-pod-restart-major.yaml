schema_version: 1
scenario_id: paymentservice-pod-restart-major
title: Payment service restart causes transient checkout failures
dataset_run_id: local-dev-run-001
traffic_profile_id: baseline-checkout-mix
environment: research-local
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components:
    - paymentservice
    - checkoutservice
    - frontend
  labels:
    - synthetic-incident
    - pod-restart
    - paymentservice
    - telemetry-linked
  summary_template: "Payment service restart caused transient checkout failures"
fault:
  fault_id: paymentservice-pod-restart-001
  fault_type: pod_restart
  affected_service: paymentservice
  suggested_mechanism: delete paymentservice pods during active checkout traffic
  expected_start_offset_seconds: 300
  expected_duration_seconds: 240
  blast_radius:
    user_visible: true
    expected_error_rate: medium
    expected_latency_impact: medium
labels:
  severity: major
  incident_type: degradation
  root_cause_category: workload_restart
  should_alert: true
  should_create_jira_shadow_issue: true
execution:
  action: RestartPods
  namespace: online-boutique-research
  target_kind: deployment
  target_name: paymentservice
  selector: app=paymentservice
triage:
  schema_version: 1
  scenario_family: payment-outage
  per_window:
    pre_fault_baseline:
      triage_label: noise
      triage_severity: null
      triage_components: null
      triage_reason_class: null
      is_hard_case: false
      rationale: "Pre-fault baseline; paymentservice healthy."
    active_fault:
      triage_label: ticket_worthy
      triage_severity: major
      triage_components:
        - paymentservice
        - checkoutservice
        - frontend
      triage_reason_class: restart_with_impact
      is_hard_case: true
      rationale: "Payment restart caused transient checkout failures. Hard case: easy to confuse with a full payment outage."
    recovery_window:
      triage_label: borderline
      triage_severity: minor
      triage_components:
        - paymentservice
      triage_reason_class: restart_with_impact
      is_hard_case: true
      rationale: "Restart usually clean once new replicas accept traffic."
