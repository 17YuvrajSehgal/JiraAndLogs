schema_version: 1
scenario_id: frontend-pod-restart-major
title: Frontend restart causes broad user-visible degradation
dataset_run_id: local-dev-run-001
traffic_profile_id: baseline-checkout-mix
environment: research-local
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components:
    - frontend
    - checkoutservice
    - productcatalogservice
  labels:
    - synthetic-incident
    - pod-restart
    - frontend
    - telemetry-linked
  summary_template: "Frontend restart caused broad customer impact"
fault:
  fault_id: frontend-pod-restart-001
  fault_type: pod_restart
  affected_service: frontend
  suggested_mechanism: delete frontend pods during active traffic
  expected_start_offset_seconds: 300
  expected_duration_seconds: 240
  blast_radius:
    user_visible: true
    expected_error_rate: medium
    expected_latency_impact: medium
labels:
  severity: major
  incident_type: degradation
  root_cause_category: workload_restart
  should_alert: true
  should_create_jira_shadow_issue: true
execution:
  action: RestartPods
  namespace: online-boutique-research
  target_kind: deployment
  target_name: frontend
  selector: app=frontend
