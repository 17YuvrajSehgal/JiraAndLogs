schema_version: 1
scenario_id: ts-loglines-test-recordonly
title: Local smoke test against the ts-loglines-test stub
description: >
  Stub scenario used only by the trainticket-localsmoke validation. Records a
  short observation window against the ts-loglines-test deployment so the
  export-telemetry-window.ps1 script can exercise the trainticket namespace
  end-to-end without needing the full train-ticket stack.
dataset_run_id: trainticket-localsmoke-001
traffic_profile_id: trainticket-booking-mix
environment: research-local
jira_candidate: false
expected_jira:
  issue_type: None
  priority: None
  components:
    - ts-loglines-test
  labels:
    - smoke-test
    - trainticket
fault:
  fault_id: null
  fault_type: none
  affected_service: ts-loglines-test
  expected_start_offset_seconds: 0
  expected_duration_seconds: 30
  blast_radius:
    user_visible: false
    expected_error_rate: very_low
    expected_latency_impact: low
labels:
  severity: none
  incident_type: baseline
  root_cause_category: none
  should_alert: false
  should_create_jira_shadow_issue: false
execution:
  action: RecordOnly
  namespace: trainticket
triage:
  schema_version: 1
  scenario_family: tt-baseline-normal
  per_window:
    observation_window:
      triage_label: noise
      triage_severity: null
      triage_components: null
      triage_reason_class: null
      is_hard_case: false
      rationale: "Smoke test observation window."
