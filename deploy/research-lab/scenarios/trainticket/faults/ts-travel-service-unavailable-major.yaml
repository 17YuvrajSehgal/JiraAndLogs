schema_version: 1
scenario_id: tt-travel-service-unavailable-major
title: Travel search service outage breaks trip lookup
description: >
  ts-travel-service is scaled to zero. /api/v1/travelservice/trips/left
  fails; booking flow can't start because users can't see available trips.
dataset_run_id: local-dev-run-001
traffic_profile_id: trainticket-booking-mix
environment: research-local
jira_candidate: true
expected_jira:
  issue_type: Incident
  priority: Major
  components:
    - ts-travel-service
    - ts-route-service
    - ts-ui-dashboard
  labels:
    - synthetic-incident
    - dependency-failure
    - ts-travel-service
    - trainticket
    - telemetry-linked
  summary_template: "Trip search outage blocked discovery and booking"
  description_template: |
    Synthetic lab issue from scenario tt-travel-service-unavailable-major.
    ts-travel-service was scaled to zero; users cannot list available trips.
fault:
  fault_id: tt-travel-service-scale-zero-001
  fault_type: service_outage
  affected_service: ts-travel-service
  suggested_mechanism: scale ts-travel-service to zero replicas
  expected_start_offset_seconds: 300
  expected_duration_seconds: 600
  blast_radius:
    user_visible: true
    expected_error_rate: high
    expected_latency_impact: medium
labels:
  severity: major
  incident_type: outage
  root_cause_category: dependency_failure
  should_alert: true
  should_create_jira_shadow_issue: true
execution:
  action: ScaleDeployment
  namespace: trainticket
  target_kind: deployment
  target_name: ts-travel-service
  replicas: 0
  restore_replicas: 1
triage:
  schema_version: 1
  scenario_family: tt-search-outage
  per_window:
    pre_fault_baseline:
      triage_label: noise
      triage_severity: null
      triage_components: null
      triage_reason_class: null
      is_hard_case: false
      rationale: "Pre-fault baseline; travel service healthy."
    active_fault:
      triage_label: ticket_worthy
      triage_severity: major
      triage_components:
        - ts-travel-service
        - ts-route-service
      triage_reason_class: dependency_failure
      is_hard_case: false
      rationale: "Trip search fails customer-visible; booking funnel blocked."
    recovery_window:
      triage_label: borderline
      triage_severity: minor
      triage_components:
        - ts-travel-service
      triage_reason_class: dependency_failure
      is_hard_case: true
      rationale: "Residual lookup misses during nacos re-registration."
