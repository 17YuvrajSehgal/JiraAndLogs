% Section 1 — Introduction
% Page budget: ~1 page (one column = ~½ page in two-column sigconf)
%
% Recommended subsection structure (introduction usually flows without subsections;
% these are the conceptual beats not LaTeX subsections):
%
%   1.1 The diagnosis bottleneck
%       - Anomaly detection is mature; engineers spend time on "have we seen this?"
%       - Past Jira tickets are an under-exploited corpus
%
%   1.2 Why this is hard
%       - No public dataset jointly contains rich telemetry + linked Jira
%       - Mature anomaly detectors already saturate detection; the next step is retrieval
%
%   1.3 Our approach in one paragraph
%       - 4-layer cascade composing 7 pipelines
%       - Each pipeline is Pareto-incomparable; the cascade picks per sub-task
%       - Locked OB v5-large dataset + OTel Demo cross-app validation
%
%   1.4 Contributions (bulleted list)
%       - A reproducible paired telemetry+Jira dataset
%       - The TCH cascade design with mathematical specification
%       - Headline retrieval, triage, and novelty metrics
%       - Cross-app generalization evidence (zero-shot + L1-retrained)
%       - Eight late-stage experiments (G-series) including three honest negative results

\section{Introduction}
\label{sec:intro}

\todo{Write introduction following the four beats above. Aim for ~600 words.
      Open with the diagnosis-bottleneck framing. State the three claims
      (retrieval scales with deployment history; cascade composition matters;
      cross-app transfer works under graded difficulty). Close with the
      contributions list. Defer all numbers to §5; the introduction names
      Hit@5 = 0.912 and novel-recall +388\% rel exactly once each.}

% Contributions — placeholder list; tighten the prose when writing
% \begin{itemize}
%   \item \textbf{Dataset.} A reproducible telemetry + Jira corpus from a forked
%         Online Boutique with bias-avoidance discipline (\S\ref{sec:dataset-construction}).
%   \item \textbf{System.} The Tiered Cascade Hybrid (TCH), a 4-layer composition
%         of 7 pipelines with a mathematical specification (\S\ref{sec:approach}).
%   \item \textbf{Results.} Hit@5 = 0.912, novel-recall = 0.793 at preserved precision,
%         16~$\mu$s per window inference (\S\ref{sec:results}).
%   \item \textbf{Cross-app evidence.} TCH transfers to a structurally different
%         microservice application (OpenTelemetry Demo) under graded difficulty
%         (L1--L4 multi-fault) without retraining (\S\ref{sec:cross-app}).
%   \item \textbf{Negative results.} Three independent rerankers all underperformed
%         the cascade's overlap-rerank, isolating a specific operating regime
%         (\S\ref{sec:results}).
% \end{itemize}
